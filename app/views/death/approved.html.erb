<!--Listing all the approved deaths information -->
<% if @approves.blank? %>
	<p><%= I18n.t "death_details_no" %>:<br>There are no Approved Birth Registration Data currently in the system.</p>
<% else %>
<!--Start of search text box -->
<%= form_tag :action => 'search' %>
	<table>
	<tr>
		<td><%= I18n.t "search" %>:<br>Search: </td>
		<td>
<%= text_field_tag :query ,@query ||="Enter Death Registration" ,:class=>'title',:onfocus=>"javascript: this.value=''",:onblur=>"javascript: if(this.value=='') this.value='Enter Death Registration'" %></td>
		<td><%= I18n.t "search" %>:<br><%= submit_tag "Search Deaths" %></td>
	</tr>
	</table>
<%= form_tag %>
<!--end of search text box -->

	<p><%= I18n.t "death_details" %>:<br>These are the current Approved :</p>
	<table>
	<tr>
	<td><%= I18n.t 'registration_number' %>:<br>Register Number</td>
 	<td><%= I18n.t 'edit' %>:<br>Edit</td>
	<td><%= I18n.t 'edit' %>:<br>Print Certificate</td>

	</tr>

	<% @approves.each do |c| %>
	<tr>
		<td><%= link_to c.register_id, {:action => 'show', :id => c.id} -%></td>
		<td><%= link_to 'Edit', {:action => 'edit', :id => c.id} %></td>
		<td><%= link_to 'Print Certificate', {:action => 'printcertificate', :id => c.id} %></td>

	</tr>
	<% end %>
	</table>
<% end %>
<br>
<table>
<tr>
<td><%= link_to I18n.t "add_new", {:action => 'new' } %><br><%= link_to "Add new", {:action => 'new' }%> </td>
<td><%= link_to I18n.t "print_report", {:action => 'deathreport' } %> <br><%= link_to "Print Report", {:action => 'deathreport' }%> </td>
</tr>
</table>

